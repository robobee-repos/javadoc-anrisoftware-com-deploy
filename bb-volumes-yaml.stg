/*
 * Mandatory:
 *
 * vars.javadoc.storage.name
 * vars.javadoc.storage.size
 *
 */

/*
 * 
 */
bb-volumes-yaml(parent, vars) ::= <<
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: <vars.javadoc.storage.name>
  namespace: javadoc-anrisoftware-com
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: <vars.javadoc.storage.size>

>>
